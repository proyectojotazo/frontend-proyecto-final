import React from "react";
import '../../sass/contenedor.scss';

class Nav extends React.Component {
    render() {
        return(
            <nav className="nav">
            Nav
            </nav>
        );
    }
}

export default Nav;